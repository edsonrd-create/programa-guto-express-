/**
 * Contratos da API (JSDoc). Export vazio: apenas referência para IDEs.
 * @typedef {object} OpsSnapshot
 * @property {object[]} orders
 * @property {object[]} deliveries
 * @property {object[]} driverQueue
 * @property {object[]} drivers
 * @property {object[]} kds
 * @property {object} ai
 * @property {string} generatedAt
 */

/** @typedef {object} AutopilotResponse
 * @property {string} mode
 * @property {object[]} actions
 * @property {string} message
 */

export {};
