# Movido

O painel operacional passou a viver em **`frontend/`** na raiz do repositório (rotas, módulos por menu, serviços e hooks).

```bash
cd frontend
npm install
npm run dev
```

Documentação: `docs/ARQUITETURA.md`.
