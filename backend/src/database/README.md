# Database

A conexão SQLite e migrações leves estão em `src/db.js` (ficheiro histórico). Evolução possível: mover para `database/connection.js` + `database/migrations/` sem alterar o nome do ficheiro `.sqlite` usado em produção.
